{
  "name": "netskillup",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "sync": "node scripts/sync-site.mjs",
    "check": "node --test tests/site.test.mjs && node scripts/validate-site.mjs && node scripts/sync-site.mjs --check",
    "test": "node --test tests/site.test.mjs",
    "test:browser": "node --test tests/browser.test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "parse5": "^8.0.1",
    "playwright": "^1.63.0"
  }
}
